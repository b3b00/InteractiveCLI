<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <fallbackPackageFolders>
    <!-- Clear any machine-level fallback folders (e.g. the VS-specific path on
         Windows) so that dotnet restore works identically on Linux/macOS/CI. -->
    <clear />
  </fallbackPackageFolders>
</configuration>
